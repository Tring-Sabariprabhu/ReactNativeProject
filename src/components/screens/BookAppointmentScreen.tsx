import { Text, View } from 'react-native';
import { styles } from 'src/assets/styles/global';

export const BookAppointmentScreen = ()=>{
    return(
        <View style={styles?.screen}>
            <Text style={styles?.paragraph}>Book Appointment Screen</Text>
        </View>
    );
};
