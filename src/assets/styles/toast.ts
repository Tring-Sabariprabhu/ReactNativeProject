import { fonts } from '../fonts';

export const toastStyle = {
    titleStyle: {
        fontSize: 22,
        fontFamily: fonts?.Medium,
    },
    textBodyStyle: {
        fontSize: 16,
        fontFamily: fonts?.Light,
    },
};
